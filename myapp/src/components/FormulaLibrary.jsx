export function FormulaLibrary({ formulas, selectedId, onSelect, icons }) {
  return <div className="formula-list">{formulas.length ? formulas.map((formula) => <button key={formula.id} className={`formula-card ${selectedId === formula.id ? 'active-card' : ''}`} onClick={() => onSelect(formula.id)}><span className={`category-dot dot-${formula.category}`}>{icons[formula.category]}</span><span className="card-info"><strong>{formula.name}</strong><small>{formula.category}</small></span><span className="card-symbol">{formula.symbol}</span><span className="card-arrow">→</span></button>) : <div className="empty-state">No formulas found. Try another search.</div>}</div>
}

export function CalculatorPanel({ formula, icon, isFavorite, onToggleFavorite, values, onValueChange, onCalculate, result, error, formattedResult, diagram }) {
  return <aside className="details-card" id="calculator"><div className="details-top"><span className={`category-dot large dot-${formula.category}`}>{icon}</span><div><span className="section-kicker">{formula.category}</span><h3>{formula.name}</h3></div><button className={`favorite ${isFavorite ? 'is-favorite' : ''}`} onClick={() => onToggleFavorite(formula.id)} aria-label="Add to favorites">{isFavorite ? '★' : '☆'}</button></div><div className="formula-display">{formula.symbol}</div>{diagram && <div className={`formula-diagram ${diagram}`}>{diagram === 'circle' && <><span className="diagram-radius">r</span><div className="diagram-circle" /></>}{diagram === 'triangle' && <><span className="diagram-base">b</span><span className="diagram-height">h</span><div className="diagram-triangle" /></>}{diagram === 'force' && <><span className="force-arrow">F →</span><div className="diagram-block">m</div></>}</div>}<p className="formula-description">{formula.description}</p><div className="learning-meta"><div><span>Meaning</span><p>{formula.meaning}</p></div><div><span>Assumptions</span><p>{formula.assumptions}</p></div><div><span>Example</span><p>{formula.example}</p></div><div><span>Difficulty</span><strong>{formula.difficulty}</strong></div>{formula.relatedFormulas.length > 0 && <div><span>Related formulas</span><p>{formula.relatedFormulas.join(' · ')}</p></div>}<div><span>Source</span><p>{formula.source}</p></div></div>{formula.id === 7 && <p className="formula-note">Real roots only. Complex roots are outside this calculator’s scope.</p>}<div className="divider" /><h4>Calculate your result</h4><div className="inputs-grid">{formula.variables.map((variable) => <label key={variable.key}><span>{variable.label}</span><div className="input-wrap"><input type="number" min={variable.min} max={variable.max} step={variable.integer ? 1 : 'any'} value={values[variable.key] || ''} onChange={(event) => onValueChange(variable, event.target.value)} placeholder="0" /><small>{variable.unit}</small></div></label>)}</div><button className="calculate-button" onClick={onCalculate}>Calculate now <span>↗</span></button>{error && <p className="error-message" role="alert" aria-live="assertive">{error}</p>}{result !== null && <div className="result-box" aria-live="polite"><span>{formula.resultLabel}</span><strong>{formattedResult} {formula.resultUnit}</strong></div>}</aside>
}
